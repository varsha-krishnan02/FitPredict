# FitPredict
A machine learning model using Support Vector Machine (SVM) with cross-validation to predict if a person is fit or unfit based on their height, weight, and gender. The tool processes user input and provides a fit prediction.
